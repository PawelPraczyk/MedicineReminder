<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExternalDevice"
             x:Class="ExternalDevice.MainPage">

    <StackLayout>
        <StackLayout>
            <ListView ItemsSource="{Binding NotificationsList}" HasUnevenRows="true" >

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="12,6">
                                <Label Text="{Binding Name}" FontSize="24"></Label>
                                <Label Text="{Binding Description}" FontSize="18" Opacity="0.6"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        
        <Button Text="Click Me" x:Name="buttonek" />
    </StackLayout>
    
</ContentPage>
